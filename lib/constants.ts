export enum DeviceType {
  COMPUTER = "COMPUTER",
  REPEATER = "REPEATER"
}

export enum CommandType {
  CREATE = "CREATE",
  MODIFY = "MODIFY",
  FETCH = "FETCH"
}

export enum Commands {
  DEVICES = "/devices",
  CONNECTIONS = "/connections",
  INFO_ROUTES = "/info-routes"
}